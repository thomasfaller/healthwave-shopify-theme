const reviewTable = $("#reviewChange-table").DataTable();
let memberCounter = 0;

const row = [
  providedFirstName,
  providedLastname,
  providedPolicy,
  providedDOB
];

reviewTable.row.add(inputRow).draw(false);
